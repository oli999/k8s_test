# 03-advertise.yaml 
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: advertise
  namespace: metallb-system
spec:
  ipAddressPools:
  - ip-pool # 02-ip-pool.yaml 파일에 작성한 ip pool 의 이름을 적는다 
